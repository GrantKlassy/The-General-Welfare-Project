---
import Base from "../../layouts/Base.astro";
import ResourceLink from "../../components/ResourceLink.astro";
import { t, localePath, type Locale } from "../../i18n";

export function getStaticPaths() {
  return [
    { params: { locale: "es" } },
    { params: { locale: "zh" } },
    { params: { locale: "vi" } },
  ];
}

const locale = Astro.params.locale as Locale;
---

<Base
  title={`${t(locale, "voting.title")} — ${t(locale, "site.title")}`}
  lang={locale}
  currentPath="/voting"
>
  <div class="mx-auto max-w-3xl px-6 py-16">
    <h1 class="text-4xl font-bold">{t(locale, "voting.title")}</h1>
    <p class="mt-4 text-lg text-gray-300">
      {t(locale, "voting.pageDesc")}
    </p>

    <section class="mt-10">
      <h2 class="text-2xl font-bold"><span class="text-flag-red">[</span> ★ {t(locale, "common.knowYourRights")} ★ <span class="text-flag-red">]</span></h2>
      <div class="mt-4 grid gap-3">
        <ResourceLink
          href="#"
          title={t(locale, "voting.registration")}
          description={t(locale, "voting.registrationDesc")}
        />
        <ResourceLink
          href="#"
          title={t(locale, "voting.voterId")}
          description={t(locale, "voting.voterIdDesc")}
        />
      </div>
    </section>

    <section class="mt-10">
      <h2 class="text-2xl font-bold"><span class="text-flag-blue">[</span> ★ {t(locale, "common.getHelp")} ★ <span class="text-flag-blue">]</span></h2>
      <div class="mt-4 grid gap-3">
        <ResourceLink
          href="#"
          title={t(locale, "voting.pollingPlace")}
          description={t(locale, "voting.pollingPlaceDesc")}
        />
        <ResourceLink
          href="#"
          title={t(locale, "voting.electionProtection")}
          description={t(locale, "voting.electionProtectionDesc")}
        />
      </div>
    </section>

    <section class="mt-10">
      <h2 class="text-2xl font-bold">[ ★ {t(locale, "common.callNow")} ★ ]</h2>
      <div class="mt-4 grid gap-3">
        <a
          href="tel:866-687-8683"
          class="block rounded-lg border border-neutral-800 px-5 py-4 transition-colors hover:border-neutral-600 hover:bg-neutral-900"
        >
          <span class="font-bold">{t(locale, "voting.phone")}</span>
          <span class="mt-1 block text-sm text-gray-400"
            >{t(locale, "voting.phoneDesc")}</span
          >
        </a>
      </div>
    </section>

    <a
      href={localePath(locale, "/")}
      class="mt-12 inline-block text-gray-400 hover:text-white"
      >&larr; {t(locale, "common.allTopics")}</a
    >
  </div>
</Base>
